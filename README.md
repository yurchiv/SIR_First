# Epidemic Simulation Web App

A stochastic epidemiological model with neural network support, built using Flask and TensorFlow. Deployable on Render.com.

## Run locally

```bash
pip install -r requirements.txt
python app.py